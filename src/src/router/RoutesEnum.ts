// All route names and paths should be here to have a single place to,
// modify all paths and simplify our code

//!Change your based on your project
export const MFE_ROUTE_NAMESPACE = "mfe";

enum ROUTES {
  ALL = "*",
  ROUTE1 = "/route1",
  ROUTE2 = "/someplace/route2"
}

export { ROUTES };
